name: test

on:
  push:
    branches: [ main_]

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run tests day1
        run: |
          cd day1
          cargo test --verbose
